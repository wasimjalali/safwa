You are the production-spec author for Ṣafwa v2, a Chrome Manifest V3 extension that cleans live Dari/Persian Q&A comments in a StreamYard studio. Produce the definitive, implementation-ready specification and WRITE it to specs/safwa-v2-architecture.md in this repository (overwrite if it exists). After writing the file, print a concise summary (max 25 lines). You have file access; modifying only specs/safwa-v2-architecture.md is allowed. Do not commit anything.

Read first, in order:
1. specs/counsel/USER-DIRECTIVE-2026-09-11.md — binding product-owner requirements (sidebar host, avatars, native panel inviolable, reliability first).
2. specs/counsel/WS-EVIDENCE-2026-09-11.md — measured WebSocket evidence (transport, schema, events), plus captures/ws-2026-09-11/streamyard-ws-evidence.json and test/fixtures/ws-2026-09-11-events.json.
3. specs/websocket-counsel-brief.md — full product/codebase context, hard constraints, the six mandatory questions.
4. specs/counsel/SYNTHESIS.md — council consensus and the conflicts that had to be resolved.
5. specs/counsel/gpt-6-astra-final-clean.md — the finalizer's architecture; specs/counsel/gpt-6-astra-revision.md — the finalizer's revision incorporating the WS evidence (this supersedes conflicting parts of the earlier final).
6. Council answers: specs/counsel/glm-5.3.md, gpt-5.6-luna.md, kimi-k3.md, qwen3.8-max.md, muse-spark-1.3.md, glm-5.3-flash.md, hy4-preview.md, grok-4.6.md.
7. Repo context: CLAUDE.md, streamyard-question-filter-spec.md, safwa-debug-handoff.md, manifest.json, package.json, src/*.js, styles.css, popup/*, test/mock-comments.js, test/run-tests.js, captures/streamyard-live-dom.json, qa-screenshots/live-2026-09-10-cli/REPORT.md.

The specification MUST be implementable without further design decisions and must contain:

A. Final architecture decision + ADRs, including: transport (DOM observer baseline vs read-only WebSocket observer — exact roles, promotion/demotion gates), panel host (chrome.sidePanel per the directive), service worker scope, state ownership (MV3 worker sleep considered), and what happens to the current popup.
B. Module layout: every new/modified file with its role and world (e.g., src/ws-main.js MAIN-world constructor tap, src/ws-bridge.js isolated-world bridge, src/ws-parser.js pure parser, src/sw.js service worker, panel/panel.html|js|css), the exact message contracts (message type names and payload shapes) between sidebar ↔ service worker ↔ content script, and the manifest diff (permissions, content_scripts entries with world/run_at, side_panel, web_accessible_resources).
C. Parser contract: pure function signature, input/output, exact field mapping from the measured schema to the plain comment objects grouping.processComment consumes ({handle, platform, displayText, timestamp, ...}), stable-id handling, duplicate-delivery suppression, pos/sentAt ordering, malformed/unknown frame behavior (never throw), and how it is unit-tested with test/fixtures/ws-2026-09-11-events.json inside npm test.
D. Sidebar UX: row anatomy (avatar, handle, platform, text, badges, count, joined state, second-question state), RTL Dari from CONFIG.LABELS, empty/loading/error/fallback states, the teacher controls (five settings + reset) moving into the sidebar, toolbar-icon behavior (opens the sidebar; no popup), and sync across StreamYard SPA re-renders.
E. Feature proxy: the exact click path (sidebar → service worker → tabs.sendMessage → content script → validated native row → click button[data-testid="show-comment-button"]), recycled-row revalidation rule, virtualized-away fallback with its exact teacher UX cost, and feature/star state reflection from commentUpdated / starredComment.starred / shownCommentIds. Never feature the wrong row; never modify the native panel.
F. Failure model table: failure → signal → threshold → fallback → teacher-visible result, covering sidebar errors, content-script loss, SPA re-render, selector rot, WS bridge silence, parse-error spike, frame flood, duplicate delivery, LLM outage. The native panel must remain untouched in every mode.
G. Config flags: CONFIG.COMMENT_SOURCE ("dom" default), WS capture/enrichment flags, demotion thresholds, and every new CONFIG.LABELS entry in Dari (with English glosses for the reader).
H. Performance budget, v2 acceptance criteria (extending v1 criteria 1–7 with panel-sidebar-specific criteria), the unit + live test plan (including the scenario suite in test/mock-comments.js), and the rollout/rollback path per the mission's evidence gates.
I. Exact amendments: the replacement wording for CLAUDE.md invariant #1 (decision record) and any spec/notes changes, plus risks ranked and remaining unknowns each with its cheapest experiment.
J. Smallest winning MVP vs later phases, with v1 behavior preserved behind flags.

Constraints: the matching core (normalize, dedup, grouping, state) stays pure (no DOM, no chrome.*); StreamYard selectors live only in src/config.js and src/dom.js; WebSocket protocol knowledge lives only in src/config.js and src/ws-parser.js; no remote code; all visible strings from CONFIG.LABELS; npm test must stay green; be decisive (no unresolved menus); label unknowns as unknowns. Write the file, then summarize.
